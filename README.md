Instalar python, una vez instalado, se debe crear un entorno virtual en la carpeta principal del proyecto de la siguiente manera,
desde la carpeta raiz del proyecto ejecutar python -m venv venv. luego activar el entorno virtual, caso linux ejecutar source venv/Scripts/activate,
caso windows, desde cmd, venv/Scripts/activate, luego ejecutar pip install django
